{
  "name": "simple-app",
  "version": "1.0.0",
  "description": "NodeJS on Alpine Docker",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build-app-image": "gulp build-app-image",
    "aws-get-login": "gulp aws-get-login",
    "run-app-in-container": "gulp run-app-in-container",
    "remove-current-app-image": "gulp remove-current-app-image",
    "push-app-image": "gulp push-app-image",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^4.0.0",
    "express": "^4.17.3",
    "gulp": "^3.9.1",
    "gulp-load-plugins": "^1.5.0",
    "gulp-shell": "^0.6.3",
    "run-sequence": "^2.2.0"
  }
}
